#include <stdio.h>
#include <stdlib.h>

// preprocessor: code that runs before the compiler
// #define creates a sequence of characters that the compiler replaces
#define DATA 100

// Treats Counter as an int
typedef int Counter;

int main(int argc, char *argv[]) {
  printf("hello world");
  return EXIT_SUCCESS;
}
